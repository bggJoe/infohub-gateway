name: Build and Deploy to Cloud Run

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

env:
  SERVICE_DIR: apps/gateway
  IMAGE_NAME: infohub-gateway

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOY_SERVICE_ACCOUNT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Set image URI
        id: image
        run: |
          echo "uri=${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GCP_ARTIFACT_REPO }}/${{ env.IMAGE_NAME }}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm ci

      - name: Run tests
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm test

      - name: Build application
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm run build

      - name: Audit dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm audit --audit-level=moderate

      - name: Build Docker image
        working-directory: ${{ env.SERVICE_DIR }}
        run: |
          docker build -t "${{ steps.image.outputs.uri }}" .

      - name: Push Docker image
        run: |
          docker push "${{ steps.image.outputs.uri }}"

      - name: Validate required workflow inputs
        run: |
          test -n "${{ vars.GCP_PROJECT_ID }}" || (echo "Missing vars.GCP_PROJECT_ID" && exit 1)
          test -n "${{ vars.GCP_REGION }}" || (echo "Missing vars.GCP_REGION" && exit 1)
          test -n "${{ vars.GCP_DEPLOY_SERVICE_ACCOUNT }}" || (echo "Missing vars.GCP_DEPLOY_SERVICE_ACCOUNT" && exit 1)
          test -n "${{ vars.GCP_RUNTIME_SERVICE_ACCOUNT }}" || (echo "Missing vars.GCP_RUNTIME_SERVICE_ACCOUNT" && exit 1)
          test -n "${{ vars.GCP_ARTIFACT_REPO }}" || (echo "Missing vars.GCP_ARTIFACT_REPO" && exit 1)
          test -n "${{ vars.CLOUD_RUN_SERVICE }}" || (echo "Missing vars.CLOUD_RUN_SERVICE" && exit 1)
          test -n "${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}" || (echo "Missing secrets.GCP_WORKLOAD_IDENTITY_PROVIDER" && exit 1)
          test -n "${{ secrets.IAP_AUDIENCE }}" || (echo "Missing secrets.IAP_AUDIENCE" && exit 1)
          test -n "${{ vars.ALLOWED_USERS }}" || (echo "Missing vars.ALLOWED_USERS" && exit 1)
          test -n "${{ vars.N8N_TIMEOUT_MS }}" || (echo "Missing vars.N8N_TIMEOUT_MS" && exit 1)
          test -n "${{ vars.N8N_MAX_RETRIES }}" || (echo "Missing vars.N8N_MAX_RETRIES" && exit 1)
      
      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ vars.CLOUD_RUN_SERVICE }}
          region: ${{ vars.GCP_REGION }}
          image: ${{ steps.image.outputs.uri }}
          flags: >-
             --service-account=${{ vars.GCP_RUNTIME_SERVICE_ACCOUNT }}
             --no-allow-unauthenticated
          env_vars: |
            AUTH_MODE=iap
            IAP_AUDIENCE=${{ secrets.IAP_AUDIENCE }}
            ALLOWED_USERS=${{ vars.ALLOWED_USERS }}
            N8N_TIMEOUT_MS=${{ vars.N8N_TIMEOUT_MS }}
            N8N_MAX_RETRIES=${{ vars.N8N_MAX_RETRIES }}
            LOG_LEVEL=info
          secrets: |
            N8N_ACTION_ITEMS_URL=N8N_ACTION_ITEMS_URL:latest
            N8N_API_AUTH_HEADER_NAME=N8N_API_AUTH_HEADER_NAME:latest
            N8N_API_AUTH_HEADER_VALUE=N8N_API_AUTH_HEADER_VALUE:latest

      - name: Show deployed service URL
        run: |
          gcloud run services describe "${{ vars.CLOUD_RUN_SERVICE }}" \
            --project="${{ vars.GCP_PROJECT_ID }}" \
            --region="${{ vars.GCP_REGION }}" \
            --format='value(status.url)'
